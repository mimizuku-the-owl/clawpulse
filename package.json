{
  "name": "clawpulse",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/convex",
    "workers/clawpulse-web",
    "tooling/typescript"
  ],
  "scripts": {
    "dev": "cd workers/clawpulse-web && bun run dev",
    "build": "cd workers/clawpulse-web && bun run build",
    "clean": "git clean -xdf node_modules dist .wrangler"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5",
    "wrangler": "^4.58.0"
  }
}
